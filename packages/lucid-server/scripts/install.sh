#!/bin/bash
# This is a copy of the root install.sh for backwards compatibility.
# The canonical version is at the repository root.

exec curl -fsSL https://raw.githubusercontent.com/JasonDocton/lucid-memory/main/install.sh | bash
